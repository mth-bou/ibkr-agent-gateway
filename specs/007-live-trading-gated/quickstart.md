# Quickstart: Live Trading Gated Enablement

This phase should be attempted only after all previous roadmap specs pass.

```bash
cargo test --workspace
```

Run the specific CLI/MCP examples introduced by this spec only after config gates and scopes are enabled.
